# 下游服務（docker-compose.yml）的設定範本。
# 用法：複製成 .env 後填入實際值 ——  cp .env.example .env
# 真正的 .env 已被 .gitignore 忽略，請勿把密鑰 commit 進版控。

# Claude 訂閱 token（claude setup-token 取得）。
# 注意：正常情況「不用手動填這裡」—— app 會由前端「設定」收 token、啟動 LLM 前自動寫入此檔。
# 這行留作手動部署／除錯備用；app 啟動 LLM 時會以設定裡的值覆寫。
# 沒有 token 時 LLM／OpenClaw 不會啟動，但錄音與轉譯照常運作。
# CLAUDE_CODE_OAUTH_TOKEN=sk-ant-oat01-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# （可選）模型，預設 opus
# CLAUDE_MODEL=opus

# （可選）claude -p 子程序逾時（毫秒），逾時即殺，預設 600000
# CLAUDE_TIMEOUT_MS=600000

# （可選）若你的 WSL uid/gid 不是 1000，改這裡（決定 openclaw 容器的執行身分與輸出檔擁有者）
# HOST_UID=1000
# HOST_GID=1000
